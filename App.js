const heading=React.createElement("h1",{id:"heading"},"hellow world React");
        const root=ReactDOM.createRoot(document.getElementById("root"));
        root.render(heading);
        console.log(heading);
